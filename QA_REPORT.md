# 社会科 基礎力マップ　改善後の再検品報告書

- 再検品日：2026年9月11日
- 対象：このフォルダの静的Webアプリ一式
- 元資料：`自習課題GPT版_検品修正版.zip` の内容を現行版と比較し、有効な変更だけを反映

## 総合判定

**授業での試用に進める状態です。**

古い検品でA判定だった、固定コースの問数増加、地図クリックの難しさ、個別指定とプリント問題の範囲ずれは修正されています。今回さらに、サーバーなしの直接起動、保存エラーへの耐性、自己採点方法の明記、誤答後の確認時間、地方・地域区分の注記を改善しました。

## 2026年9月11日の品質改善

- 日本プリントにも世界プリントと同じ番号表示を追加し、地図上の1〜3番と同じ番号の欄へ都道府県名を書く形式にした。
- 日本プリントで複数地方を選んだ場合、地方名欄を地方数に合わせて表示。
- 日本プリントの選択地方を色A・B・Cに分け、県境線を残して隣接地方を判別しやすくした。
- 世界プリントで地域を3つまで複数選択できるようにし、地域別の色分けと凡例を追加。
- 4択の誤答を同じ地方・近い地域から優先して選び、全国・全世界からの不自然な無作為選択を削減。
- 日本・世界の「正しい組み合わせ」で、誤答3個が同じ県庁所在地・首都に偏る問題を修正。
- 世界地図○×の誤位置は、原則として同じ地域内の別の主要国を使うよう修正。
- 実際の難しさに合わせ、単純な地図照合3形式を「考える」から「少し考える」へ変更。
- `tests/quality-check.js` を追加し、全テンプレート・全対象・複数地域プリントを自動検査。

自動検査では、全26形式×全対象×8回、20問コース40回、日本4条件×30回、世界4条件×30回を生成し、正解の一意性・選択肢重複・○×比率・同一文重複・組み合わせ問題を確認して合格しました。ブラウザでは東北＋関東、東アジア＋東南アジア、3地域上限、10問完走を確認しています。

## 今回反映した改善

- `index.html` をダブルクリックして起動できる構成へ変更。
- 日本・世界のSVG地図を `maps-inline.js` にまとめ、`file://` 起動時の読み込み制限を回避。
- `start-app.cmd` も、複雑なローカルサーバーを使わずに起動できるよう変更。
- `localStorage` が利用できない端末でも、保存処理の例外でクイズが止まらないよう修正。
- 不正解・「わからない」のあと、正解を読む時間を約1.8秒へ延長。
- 日本プリントの特徴問題に「3つすべて合って1問」など、自己採点の数え方を明記。
- 地方・地域区分は、このアプリに表示する8区分へ統一することを画面に明記。
- 世界プリントの解答表の文字を少し大きくした。

## 再検品結果

| 項目 | 結果 |
|---|---|
| JavaScript構文 | `app.js`、データ3ファイル、`maps-inline.js` はすべて構文検査通過 |
| HTMLのID | 57件、重複0件 |
| 起動構成 | ES Modulesと地図の`fetch()`を使わない構成へ変更 |
| ローカルサーバー | `http://127.0.0.1:8000/` でHTTP 200 |
| 10問コース | 全問「わからない」でも10問で終了し、0/10・0%を表示 |
| 日本地図 | 四国指定で拡大表示。県を押す問題を表示 |
| 日本プリント | 東北指定で3ページ、地図1枚、採点方法の注記あり |
| 世界プリント | ヨーロッパ指定で4ページ、全体図と拡大図の2枚、解答表あり |
| スマートフォン幅 | ヨーロッパプリントの見出し・地図が画面内に表示 |
| デスクトップコピー | 主要9ファイルのSHA-256が作業版と一致 |

## 旧QAでA判定だった3点

### A-1 固定問数が誤答で増え続ける

**修正済み。** 固定コースでは復習問題を既存枠へ入れ、コース末尾を越える場合は追加しません。再検品でも10問で終了しました。

### A-2 地図クリックのタップ範囲が小さい

**修正済み。** 地域ごとの拡大表示と、小さい県・国へのクリック補助を入れています。学校PCやChromebookでは、香川・台湾・韓国などを実機で1回確認するとより安全です。

### A-3 プリントの個別指定と○×問題の範囲が一致しない

**修正済み。** 選んだ都道府県を問題生成へ渡し、複数地方では地方ごとに偏りにくくしています。

## 公開前に実機で確認したいこと

- 学校で使うChrome／Chromebookから `index.html` を開き、トップの2ボタンが動くか。
- 東北とヨーロッパを各1部PDF保存または紙印刷し、余白と改ページを確認する。
- 国旗は絵文字なので、学校端末やモノクロ印刷での見え方を確認する。

## 今後の改善候補

- `app.js` は大きいため、問題形式をさらに増やす段階で、クイズ生成とプリント生成を別ファイルへ分ける。
- 国旗を端末差の少ない画像へ置き換える場合は、利用条件を確認した素材を `assets` フォルダへ追加する。

以前の詳細な検品記録は `QA_REPORT_20260909_旧版.md` に残しています。
