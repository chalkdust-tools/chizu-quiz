# 社会科 基礎力マップ　公開前QA検品報告書

- 検品日：2026年9月9日
- 対象：このフォルダにある静的Webアプリ一式
- 検品方針：コードは変更せず、実際のブラウザ操作、データ照合、ランダム生成の繰り返し、画面幅変更、コード構造確認を行った。

## 総合判定

**現状は「主要機能は動くが、Aの3項目を直すまでは公開・授業使用を推奨しない」状態。**

トップ画面、通常の4択、○×、地図表示、結果画面、日本・世界プリント生成、自己採点ページは概ね正常に動いた。問題文も短く、「わからない」があり、低学力層でも始めやすい良さがある。一方で、誤答した生徒ほどコースが大幅に長くなること、スマートフォンでは地図クリックが物理的に難しいこと、プリントの個別範囲指定が○×問題に反映されないことは、実使用前に直す必要がある。

## 検品結果の概要

| 検品項目 | 結果 |
|---|---|
| ローカル起動 | `http://127.0.0.1:8000/` で表示。主要HTML・CSS・JS・地図SVGはすべてHTTP 200 |
| ブラウザエラー | 一連の操作後もコンソールエラー 0件 |
| 日本データ | 47都道府県、コード・名称・県庁所在地に重複なし。8地方の件数と対応も照合済み |
| 世界データ | 39か国。必須項目・地図IDの欠落、国名・国旗の重複なし |
| 問題形式 | 日本・世界合計26テンプレート |
| 10/20/30問 | 開始時はそれぞれ 6:3:1、12:6:2、18:9:3 の難易度比率 |
| ランダム耐久 | 日本30セット、世界30セットを生成。各条件内で全セットが別内容、所定問数・難易度比率も正常 |
| 日本プリント | 全8地方を生成。東北・関東・九州・沖縄は各3回。すべて3ページ、同条件3回は別内容 |
| 世界プリント | 世界全体＋8地域を生成。東アジア・ヨーロッパは各3回。すべて4ページ、同条件3回は別内容 |
| プリント問題 | 基本問題は各4択・選択肢重複なし・解答記号と一致。○×は10問、○6・×4、訂正文あり |
| A4収まり | 画面上のA4相当サイズは各ページ約794×1123px。日本・世界とも内容によるページ高さ超過なし |
| スマートフォン | クイズ・トップは横はみ出しなし。プリント見出しと地図クリックに問題あり |

### 追加の全件整合性検査

- `app.js`、`data/prefectures.js`、`data/countries.js`、`data/questions.js` は、JavaScript構文検査をすべて通過した。
- `data/questions.js` の26テンプレートはすべてIDが一意で、`app.js` 側にも対応する26個の生成処理がある。生成処理の欠落・孤立は0件。
- `index.html` の静的IDは55個ですべて一意。文字のない操作ボタンは0件。
- ブラウザで26形式を単独条件にして連続巡回する追加検査は、検品用ブラウザの1回30秒の操作制限で途中終了した。そのため、形式の全件確認は上記の静的照合と、日本30セット・世界30セットの実生成結果を組み合わせて判定した。

## A：公開・授業使用前に必ず直す

### A-1　固定問数コースが、誤答によって大幅に長くなる

- **何が問題か**：10問コースで「わからない」を続けると、再出題を入れるたびに全体の問数が増える。
- **どこで起きるか**：`app.js` の `scheduleAlternateRetry()`（500行付近）。3〜5問後の位置がコース末尾を越えると、穴埋め問題と再出題を追加している。
- **実測**：10問コースで20回「わからない」を押すと、進行表示が `1/10 → 2/11 → … → 20/21` となり、20問答えても終了しなかった。
- **影響**：最も苦手な生徒ほど課題が長くなり、「10問なら終えられる」という見通しを失う。授業の残り時間にも収まりにくい。
- **直し方**：再出題は最初の10問の中の予約枠に入れるか、通常コースでは追加を最大2〜3問までにする。表示する分母は固定し、追加復習は結果画面の後に任意で行う形がよい。

### A-2　地図クリックのタップ範囲が小さすぎる

- **何が問題か**：国・都道府県の実際の輪郭だけがクリック範囲で、スマートフォンでは数ピクセルしかない対象がある。
- **どこで起きるか**：日本・世界の「県名／国名 → 地図クリック」。`setupJapanMapClick()`、`setupWorldMapClick()`。
- **実測**：390px幅では、世界地図の台湾が約3.1×3.4px、韓国が約3.6×5.6px、バングラデシュが約4.7×6.0px。日本地図でも香川県の高さが約7.7px、東京都が約10.9pxだった。
- **影響**：場所を知っていても正しく押せない。低学力層には「知識ではなく操作で間違えた」と感じさせ、スマートフォン要件を満たさない。
- **直し方**：出題対象地域へ自動ズームする。小さい国・県には見えない大きめの当たり判定、番号付き吹き出し、または地図横の候補ボタンを用意する。最低でも小国・狭い県を全世界図の輪郭だけで押させない。

### A-3　プリントの範囲指定と、実際の○×問題の範囲が一致しない

- **何が問題か**：都道府県を1つだけ選んでも、○×問題はその県ではなく地方全体から作られる。複数地方を選んだ場合も、○×9問が最初の地方に偏り、最後の1問だけが2地方を扱う。
- **どこで起きるか**：日本プリントの `buildPrintOx(regions)`。選択した都道府県一覧ではなく、最初の地方名だけを主に使っている。
- **実測**：山形県だけを選ぶと、地図は山形県だけを強調したが、○×には秋田県、青森県、福島県、宮城県などが出た。東北＋関東を選ぶと、10問中9問が東北中心だった。
- **影響**：教員が「山形県だけ」「東北と関東」と指定したつもりでも、未習範囲や偏ったプリントを配ることになる。範囲指定への信頼を損なう。
- **直し方**：`buildPrintOx()` に選択された都道府県そのものを渡す。個別選択時は原則その県から作り、複数地方時は各地方から同数程度を出す。

## B：できれば直す

### B-1　クイズ中に「戻る」「やめる」がなく、終了操作の意味が分かりにくい

- **問題**：クイズ中の操作は「結果を見る」だけ。未回答のまま押すと結果画面を出さずトップへ戻り、1問以上回答後は途中結果を出す。
- **影響**：同じボタンが回答数によって違う動きをし、生徒や教員が迷う。ブラウザの戻る操作では学習状態も保証されない。
- **改善**：「やめる」を明示し、「途中結果を見る」「トップへ戻る」のどちらかを確認できるようにする。範囲選択へ戻るボタンもあるとよい。

### B-2　狭い範囲では同じ問題文が何度も出る

- **問題**：候補を使い切ると、同じ問題をそのまま再利用する。
- **実測**：「日本地図＋北海道＋やさしい＋10問」は、知識1件×形式2件の計2種類しかなく、10問中に同一組み合わせの連続が3回あった。「世界地図＋東アジア＋やさしい＋10問」も形式はすべて同じだった。
- **影響**：「手を変え品を変え」の狙いが弱くなり、作業的になる。
- **改善**：候補が少ない条件では開始前に案内する、問数を候補数に合わせる、または地方・県庁所在地など別分野を自動的に混ぜる選択肢を出す。

### B-3　ロシアが世界プリントから完全に除外される

- **問題**：`printPrimary: false` のため、ロシアはヨーロッパだけでなく「世界全体」のプリント問題にも出ない。クイズには出る。
- **影響**：主要国として扱う方針と、クイズ・プリントの内容が一致しない。
- **改善**：地域拡大範囲の計算からだけ除外し、出題候補からは除外しないよう、ズーム用フラグと出題用フラグを分ける。

### B-4　スマートフォンのプリント画面で題名が縦に細かく折れる

- **問題**：390px幅で、プリントの題名が「ヨーロッパ 世界地理プリント…」の各語ごと、場合によっては数文字ごとに縦へ折り返される。氏名欄と題名が横並びのまま縮むため。
- **影響**：生成結果の確認が非常に読みにくい。印刷したA4は正常でも、スマートフォン上のプレビュー品質が低い。
- **改善**：スマートフォン時だけプリント見出しを縦並びにし、題名と氏名欄をそれぞれ幅100%にする。

### B-5　日本プリントの自己採点の数え方が明記されていない

- **問題**：「特徴を3つ選ぶ」を、3答として数えるのか、全て合って1問として数えるのか不明。現在の合計点は、この欄全体を1問として計算している。
- **影響**：生徒ごとに正解数が変わり、自己採点が統一されない。
- **改善**：「3つすべて合って1問」など採点方法を書くか、各選択肢を1点として合計問数を調整する。

### B-6　問題テンプレートの実体が1ファイルに分離し切れていない

- **問題**：`data/questions.js` には26形式の名前・難易度があるが、実際の問題文、選択肢、正解を作る処理は約65KBの `app.js` の `makeQuestion()` にある。プリント生成も同じファイルにまとまっている。
- **影響**：初心者が形式を追加する場合、2ファイルを対応させる必要があり、修正事故が起きやすい。
- **改善**：少なくともクイズの生成関数を `data/question-generators.js` などへ分け、テンプレートIDと生成処理を近くに置く。データ項目の意味もREADMEに表で書く。

### B-7　ローカル起動がPython依存で、起動直後に一度エラー表示になる可能性がある

- **問題**：`start-app.cmd` はサーバーを開始する前にブラウザを開く。PCにPythonがない場合は起動できず、あってもサーバー準備前にページが開くことがある。
- **影響**：初心者には「壊れている」と見えやすい。
- **改善**：公開時はGitHub PagesのURLを基本にする。ローカル用はサーバー開始を確認してからURLを開く仕組みにする。

### B-8　自動テストと戻せる版がない

- **問題**：現在のGit管理では全ファイルが未追跡で、正常版の履歴がない。データ件数、正解、ランダム生成、印刷ページ数を自動確認するテストもない。
- **影響**：今後「国を追加」「問題形式を追加」した際に、既存機能の破損を見落としやすく、元に戻しにくい。
- **改善**：現状を初回コミットとして保存し、最低限のデータ検査と30回生成テストを自動化する。

## C：余裕があれば改善

### C-1　不正解の答え表示が1.2秒で消える

- **問題**：短い説明ではあるが、読むのが遅い生徒には「正解＋一言」を読み切れない場合がある。
- **改善**：1.8〜2.0秒程度にするか、「次へ」を押して進める設定を選べるようにする。ただしテンポは維持する。

### C-2　世界プリント解答欄の文字がやや小さい

- **問題**：解答表は8.5pt、出典は7.5pt。A4には収まるが、低学力層や視力が弱い生徒には小さめ。
- **改善**：問題数や余白を微調整し、解答表を9〜9.5ptへ上げられるか確認する。

### C-3　国旗は絵文字表示に依存する

- **問題**：今回のWindows環境では正常表示したが、古いChromebook、モノクロ印刷、端末フォントによって見え方が変わる可能性がある。
- **改善**：実際に使う学校端末で確認し、必要ならライセンスを確認した国旗SVGへ切り替える。

### C-4　地域区分の約束を画面上でも一言示すと安全

- **問題**：ロシアをヨーロッパ、三重県を近畿として扱うなど、複数の分け方がある項目がある。データ内では統一されているが、生徒画面では約束が目立たない。
- **改善**：「このアプリではこの区分で出題します」を範囲選択の近くに短く表示する。

### C-5　地域を絞った世界クイズで、問題形式が1度だけ連続した

- **実測**：東アジア20問を10セット生成したうち1セットで、同じテンプレートが隣り合った。知識対象そのものの連続はなかった。
- **改善**：最終並べ替え時に隣接テンプレートをもう一度検査する。

### C-6　世界地図SVGが大きく、プリントでは同じIDを持つSVGを2枚置く

- **問題**：世界SVGは約1.1MBで、プリント1ページに2コピー入る。画面読み上げでは多数の国・地域名が展開され、DOM内には重複IDもできる。
- **影響**：今回のPCでは止まらなかったが、低性能端末、保守性、読み上げ操作では負担になる可能性がある。
- **改善**：装飾用地図を読み上げ対象から外す、必要地域だけのSVGを作る、挿入時に内部IDを固有化する。

## 機能別の確認詳細

### クイズ

- トップからクイズ、範囲選択、10/20/30問、ランダム、エンドレス、復習へ移動できた。
- 通常の4択、○×、「わからない」、自動次問、途中結果、もう一度、間違い復習は動作した。
- 日本地図クリックは東京都、世界地図クリックは日本を実際に選び、正解判定を確認した。
- 日本の全8地方、世界の全8地域で地図問題を生成し、地図表示と対象データの一致を確認した。
- 初期セットでは難易度60%・30%・10%が正確だった。
- 幅広い条件でのランダム性は良好。ただし狭い範囲指定ではB-2が起きる。
- 得点が二重加算される操作は見つからなかった。回答直後は入力がロックされる。

### データ

- 日本は47件、都道府県コード・名称・県庁所在地が各47件で重複なし。
- 地方件数は、北海道1、東北6、関東7、中部9、近畿7、中国5、四国4、九州・沖縄8。
- 世界は39か国。地域件数は東アジア6、東南アジア6、南アジア3、ヨーロッパ9、北アメリカ3、南アメリカ4、アフリカ6、オセアニア2。
- 全39か国の地図IDはSVG内に存在した。
- 首都移転中のインドネシアと、首都機能が3都市に分かれる南アフリカ共和国を1首都回答問題から外している点は適切。南アフリカの扱いは同国政府情報とも一致する。
- シンガポール、オランダを全世界地図クリックから外している判断は、小ささを考えると妥当。ただし他にもA-2の小対象が残る。

### 問題品質

- 30セットずつの生成とコード確認では、4択の正解なし、正解2つ、選択肢重複、解答記号の不一致は見つからなかった。
- プリントの「3つ選ぶ」は、単一地方では正答候補が正確に3つ。複数地方時は「すべて選ぶ」に変わる。
- プリント○×は毎回、やさしい6、少し考える3、考える1。○6・×4で、×問題には訂正文がある。
- 誤答は地方違い、同地域の別首都、県数違いなどが混ざり、「明らかに変な地名だけ」には偏っていない。

### 自習プリントと地図

- 日本は全8地方で地図表示、県境、色分けを確認。色付き県数も地方件数と一致した。
- 世界は世界全体、東アジア、東南アジア、南アジア、ヨーロッパ、北アメリカ、南アメリカ、アフリカ、オセアニアを確認。
- 世界の地域版は世界全図と地域拡大図の2段構成。拡大図の番号はすべて表示範囲内で、周辺の海・隣接地域も残っていた。
- 日本は3ページ、世界は4ページ。問題、○×、解答が別ページになり、画面上の操作ボタンには `no-print` が付いている。
- `@page A4 portrait`、ページ改行、問題ブロックの途中分割防止CSSを確認した。
- 印刷ボタンは反応し、`window.print()` 呼び出しも確認した。ただし自動操作環境ではOSの印刷ダイアログ自体を読み取れなかった。**公開前に、実際に使うChrome／Chromebookで東北・ヨーロッパ各1部をPDF保存または紙印刷し、余白と空白ページを最終確認すること。**

### 対象生徒目線

良い点は次のとおり。

- トップの「今日の10問」から説明なしで始められる。
- クイズは文字入力がなく、4択・○×・クリック中心。
- 問題文は短く、検査した先頭問題の最大も20文字程度だった。
- 「わからない」が常にあり、正解と短い補足が出る。
- 大きめの選択肢、スマートフォンでの横はみ出しなし、結果と間違い一覧がある。
- プリントは「予想でもOK」「一言でOK」と書かれ、自己採点まで一人で進めやすい。

ただしA-1、A-2、A-3は、苦手な生徒ほど強く影響を受けるため優先修正が必要。

## 保守性の判定

- 知識データは `data/prefectures.js`、`data/countries.js`、地域特徴は `data/questions.js` に分かれており、県庁所在地や国を直す場所は分かりやすい。
- 地図ID、`capitalQuiz`、`mapQuiz`、`printPrimary` の意味はコメントがあり、今回の特殊国の扱いも追いやすい。
- 一方、問題形式追加は `data/questions.js` と `app.js` の両方を触る必要がある。今後Codexに依頼することはできるが、初心者が単独で増やすにはやや難しい。
- 公開前にGitで正常版を1回保存し、変更後にこの報告書と同等の検査を再実行できる状態にするのが安全。

## 修正後の再検品で必ず確認すること

1. 10問コースを全問不正解にしても、上限どおりに終了する。
2. 台湾・韓国・香川県・東京都などをスマートフォンで無理なく押せる。
3. 山形県だけを選んだプリントに、未選択県中心の問題が出ない。
4. 東北＋関東の○×が両地方からほぼ均等に出る。
5. 北海道＋日本地図＋やさしい＋10問で、同一文の連続が起きないか、事前に候補不足を案内する。
6. ロシアが世界全体・ヨーロッパのプリント方針どおりに扱われる。
7. 390px幅でプリント題名が普通に読める。
8. 実際の学校用Chrome／Chromebookの印刷プレビューで、空白ページ・切れ・国旗欠けがない。
